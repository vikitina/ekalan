{% extends 'layout/admin.twig' %}


{% block content %}
<a href="/admin/msgs">Messages</a> / {{ msg.date }}
<hr>
<div class="row">
     <div class="col-md-8">
                     <section class="">
                            <div class="panel panel-default">
                                      <div class="panel-heading">
        
                                              <h4>{{ msg.date }}</h4>
                                       </div>
                                          <div class="panel-body">
                                                    <div class="alert alert-info">
                                                           <button type="button" class="close" data-dismiss="alert">×</button>
                                                         This message will be checked as read
                                                   </div>
                                                     <p>{{ msg.name }}</p>
                                                     <p>{{ msg.company }}</p>
                                                     <p>{{ msg.phone }}</p> 
                                                     <p>{{ msg.email }}</p> 
                                                     {{ msg.content|raw }}
                                                   
                                                  
                                         </div>
                                 </div>   	



			        </section>
     </div>       

{% endblock content %}